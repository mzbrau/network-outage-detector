# Documentation Site

This folder contains the Docusaurus site for the Network Outage Detector project.

## Local development

```bash
cd docs
npm install
npm run start
```

## Production build

```bash
cd docs
npm run build
```

The generated static site is written to `docs/build`.
